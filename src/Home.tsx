import React, { FC } from 'react';
import ProfilePage from './ProfilePage';

const Home: FC = () => {
  return (
    <div className="Home">
      <ProfilePage />
    </div>
  );
};

export default Home;
